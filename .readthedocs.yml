# Required
version: 2

# Image to use
build:
  image: testing

  
# Set the version of Python
build:
  os: ubuntu-20.04  # <- add this line


# Configuration
python:
  version: 3.9
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
